(function() {
    var sale = {
        roomRate: 99,
        discount: 9,
        offerPrice: function() {
            var offerRate = this.roomRate * ((100 - this.discount) / 100);
            return offerRate;
        }
    };
    var hotelName, roomRate, specialRate;
    hotelName = document.getElementById('hotelName');
    roomRate = document.getElementById('roomRate');
    specialRate = document.getElementById('specialRate');
    hotelName.textContent = sale.name;
    roomRate.textContent = '$' + sale.roomRate.toFixed(2);
    specialRate.textContent = '$' + sale.offerPrice();
}());
(function() {
    var sale = {
        roomRate: 99,
        discount: 9,
        offerPrice: function() {
            var offerRate = this.roomRate * ((100 - this.discount) / 100);
            return offerRate;
        }
    };
    var hotelName, roomRate, specialRate;
    hotelName = document.getElementById('hotelName');
    roomRate = document.getElementById('roomRate');
    specialRate = document.getElementById('specialRate');
    hotelName.textContent = sale.name;
    roomRate.textContent = '$' + sale.roomRate.toFixed(2);
    specialRate.textContent = '$' + sale.offerPrice();
}());
(function() {
    var sale = {
        roomRate: 99,
        discount: 9,
        offerPrice: function() {
            var offerRate = this.roomRate * ((100 - this.discount) / 100);
            return offerRate;
        }
    };
    var hotelName, roomRate, specialRate;
    hotelName = document.getElementById('hotelName');
    roomRate = document.getElementById('roomRate');
    specialRate = document.getElementById('specialRate');
    hotelName.textContent = sale.name;
    roomRate.textContent = '$' + sale.roomRate.toFixed(2);
    specialRate.textContent = '$' + sale.offerPrice();
}());
(function() {
    var sale = {
        roomRate: 99,
        discount: 9,
        offerPrice: function() {
            var offerRate = this.roomRate * ((100 - this.discount) / 100);
            return offerRate;
        }
    };
    var hotelName, roomRate, specialRate;
    hotelName = document.getElementById('hotelName');
    roomRate = document.getElementById('roomRate');
    specialRate = document.getElementById('specialRate');
    hotelName.textContent = sale.name;
    roomRate.textContent = '$' + sale.roomRate.toFixed(2);
    specialRate.textContent = '$' + sale.offerPrice();
}());
(function() {
    var sale = {
        roomRate: 99,
        discount: 9,
        offerPrice: function() {
            var offerRate = this.roomRate * ((100 - this.discount) / 100);
            return offerRate;
        }
    };
    var hotelName, roomRate, specialRate;
    hotelName = document.getElementById('hotelName');
    roomRate = document.getElementById('roomRate');
    specialRate = document.getElementById('specialRate');
    hotelName.textContent = sale.name;
    roomRate.textContent = '$' + sale.roomRate.toFixed(2);
    specialRate.textContent = '$' + sale.offerPrice();
}());